{
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "# 04 - Validation: Leave-One-Subject-Out Cross-Validation\n",
    "\n",
    "## Objective\n",
    "LOSO cross-validation with comprehensive metrics (Koelstra et al. 2011 standard)\n",
    "\n",
    "## Scientific References\n",
    "- **LOSO Standard**: Koelstra et al. (2011) https://doi.org/10.1109/TAFFC.2011.15\n",
    "- **Baseline SOTA**: Wang et al. (ICLR 2025) CBraMod 94.5% valence\n",
    "- **Few-Shot Baseline**: Kim et al. (2024) EEG-GPT 85-90%"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "import numpy as np\n",
    "from pathlib import Path\n",
    "from sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score, confusion_matrix, cohen_kappa_score\n",
    "\n",
    "print(\"=\"*70)\n",
    "print(\"04 - VALIDATION: Leave-One-Subject-Out (LOSO)\")\n",
    "print(\"Reference: Koelstra et al. (2011) DEAP Standard\")\n",
    "print(\"=\"*70)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Load predictions for all subjects\n",
    "pred_dir = Path('./predictions')\n",
    "all_val_pred, all_val_true = [], []\n",
    "all_arous_pred, all_arous_true = [], []\n",
    "all_emotion_pred, all_emotion_true = [], []\n",
    "\n",
    "print(\"\\n[Loading] Predictions from all subjects...\")\n",
    "for subject_id in range(1, 33):\n",
    "    pred_file = pred_dir / f'subject_{subject_id:02d}_predictions.npz'\n",
    "    if pred_file.exists():\n",
    "        data = np.load(pred_file)\n",
    "        all_val_pred.extend(data['valence_pred'])\n",
    "        all_val_true.extend(data['valence_true'])\n",
    "        all_arous_pred.extend(data['arousal_pred'])\n",
    "        all_arous_true.extend(data['arousal_true'])\n",
    "        all_emotion_pred.extend(data['emotion_pred'])\n",
    "        all_emotion_true.extend(data['emotion_true'])\n",
    "        data.close()\n",
    "\n",
    "val_pred = np.array(all_val_pred)\n",
    "val_true = np.array(all_val_true)\n",
    "arous_pred = np.array(all_arous_pred)\n",
    "arous_true = np.array(all_arous_true)\n",
    "emotion_pred = np.array(all_emotion_pred)\n",
    "emotion_true = np.array(all_emotion_true)\n",
    "\n",
    "print(f\"✓ Loaded {len(val_pred)} trials from 32 subjects\")"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},\n",
   "outputs": [],\n",
   "source": [
    "# Valence Classification Metrics\n",
    "print(\"\\n\" + \"=\"*70)\n",
    "print(\"VALENCE CLASSIFICATION (High/Low)\")\n",
    "print(\"=\"*70)\n",
    "\n",
    "val_acc = accuracy_score(val_true, val_pred)\n",
    "val_f1 = f1_score(val_true, val_pred, average='weighted')\n",
    "val_kappa = cohen_kappa_score(val_true, val_pred)\n",
    "val_cm = confusion_matrix(val_true, val_pred)\n",
    "\n",
    "print(f\"\\nAccuracy: {val_acc:.2%}\")\n",
    "print(f\"F1-Score: {val_f1:.2%}\")\n",
    "print(f\"Kappa: {val_kappa:.4f}\")\n",
    "print(f\"\\nConfusion Matrix:\\n{val_cm}\")"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},\n",
   "outputs": [],\n",
   "source": [
    "# Arousal Classification Metrics\n",
    "print(\"\\n\" + \"=\"*70)\n",
    "print(\"AROUSAL CLASSIFICATION (High/Low)\")\n",
    "print(\"=\"*70)\n",
    "\n",
    "arous_acc = accuracy_score(arous_true, arous_pred)\n",
    "arous_f1 = f1_score(arous_true, arous_pred, average='weighted')\n",
    "arous_kappa = cohen_kappa_score(arous_true, arous_pred)\n",
    "arous_cm = confusion_matrix(arous_true, arous_pred)\n",
    "\n",
    "print(f\"\\nAccuracy: {arous_acc:.2%}\")\n",
    "print(f\"F1-Score: {arous_f1:.2%}\")\n",
    "print(f\"Kappa: {arous_kappa:.4f}\")\n",
    "print(f\"\\nConfusion Matrix:\\n{arous_cm}\")"
   ]
  },\n",
  {
   "cell_type": "code",\n",
   "execution_count": null,\n",
   "metadata": {},\n",
   "outputs": [],\n",
   "source": [
    "# 4-Class Emotion Classification\n",
    "print(\"\\n\" + \"=\"*70)\n",
    "print(\"EMOTION CLASSIFICATION (4-Class)\")\n",
    "print(\"=\"*70)\n",
    "\n",
    "emotion_names = ['Sad (Low-Low)', 'Calm (Low-High)', 'Angry (High-Low)', 'Happy (High-High)']\n",
    "emotion_acc = accuracy_score(emotion_true, emotion_pred)\n",
    "emotion_f1 = f1_score(emotion_true, emotion_pred, average='weighted')\n",
    "emotion_kappa = cohen_kappa_score(emotion_true, emotion_pred)\n",
    "\n",
    "print(f\"\\nAccuracy: {emotion_acc:.2%}\")\n",
    "print(f\"F1-Score: {emotion_f1:.2%}\")\n",
    "print(f\"Kappa: {emotion_kappa:.4f}\")\n",
    "\n",
    "print(f\"\\n🎯 COMPARISON TO BASELINES:\")\n",
    "print(f\"  Random Chance (4-class): 25.0%\")\n",
    "print(f\"  EEG-GPT Few-Shot (Kim): 85-90%\")\n",
    "print(f\"  CBraMod SOTA Valence (Wang): 94.5%\")\n",
    "print(f\"  This Work: {emotion_acc:.2%}\")\n",
    "\n",
    "print(f\"\\n\" + \"=\"*70)\n",
    "print(f\"✓ Validation complete - Ready for output analysis\")\n",
    "print(f\"=\"*70)"
   ]
  }
 ],\n",
 "metadata": {\n",
  "kernelspec\": {\n",
   \"display_name\": \"Python (eeg_LLM_env)\",\n",
   \"language\": \"python\",\n",
   \"name\": \"eeg_llm_env\"\n",
  },\n",
  \"language_info\": {\n",
   \"name\": \"python\",\n",
   \"version\": \"3.10.0\"\n",
  }\n },\n \"nbformat\": 4,\n \"nbformat_minor\": 4\n}
